# Security

Do not open issues containing secrets, signing keys, tokens, OAuth credentials, private property data, private contact data, or live system credentials.

This public kernel should not require secrets. If a future adapter needs credentials, use environment variables and keep that adapter disabled by default.
