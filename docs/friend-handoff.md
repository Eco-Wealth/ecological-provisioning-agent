# Developer Handoff

Build this as a brandless public kernel.

Do not add private brand names, transaction-signing logic, payment execution, dispatching, deployment automation, public posting, credentials, or claims of certification.

Focus on making the local loop work cleanly:

```text
intent -> route -> packet -> proof -> receipt -> validate
```

Keep commits small and receipts clear.
